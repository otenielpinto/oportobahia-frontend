# Generate a random secret: openssl rand -base64 32

# App data MongoDB
MONGO_CONNECTION=
MONGO_DATABASE=

# Auth MongoDB (separate from app data)
NEXT_AUTH_SECRET=
NEXT_AUTH_MONGO_URL=
NEXT_AUTH_MONGO_DATABASE=

# Optional: redirect after sign-in
NEXT_PUBLIC_KOMACHE_AFTER_SIGN_IN_URL=/home



